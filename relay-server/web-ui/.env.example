# Relay WebSocket URL for browser connection (optional, has default)
# VITE_RELAY_URL=ws://localhost:8080/browser

# Relay server port (optional, defaults to 8080)
# RELAY_PORT=8080

# Web server port (optional, defaults to 3000)
# PORT=3000
